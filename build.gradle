plugins {
    id "com.diffplug.spotless" version "5.1.0"
    id "java"
}

repositories {
    mavenCentral()
}

subprojects {
    apply plugin: 'com.diffplug.spotless'
    spotless {
        java {
            googleJavaFormat()
        }
    }
}